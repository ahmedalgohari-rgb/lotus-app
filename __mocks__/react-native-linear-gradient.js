import React from 'react';
import { View } from 'react-native';

// Mock the LinearGradient component as a simple View
const LinearGradient = (props) => {
  return <View {...props} />;
};

export default LinearGradient;